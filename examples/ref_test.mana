module ref_test;

struct Point {
    x: i32;
    y: i32;
}

fn print_point(p: &Point) -> void {
    print("Point: ");
    println(p.x);
}

fn main() -> i32 {
    let p = Point{x: 10, y: 20};
    print_point(&p);
    return 0;
}
