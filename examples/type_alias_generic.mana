module type_alias_generic;

type IntVec = Vec<i32>;

fn main() -> i32 {
    let mut vec: IntVec = Vec<i32>{};
    vec.push(1);
    vec.push(2);
    vec.push(3);
    println(vec.len());
    return 0;
}
