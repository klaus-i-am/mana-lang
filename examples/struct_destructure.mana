module struct_destructure;

struct Point {
    x: i32,
    y: i32
}

fn main() -> i32 {
    let p: Point = Point { x: 10, y: 20 };

    // Struct destructuring
    {x, y}: Point = p;
    println("x={}, y={}", x, y);

    return 0;
}
