module main;

fn main() -> i32 {
    // Test default capture by reference
    let mut x: i32 = 10;
    let add_x = |n: i32| x + n;
    println(add_x(5));

    // Test capture by value with move
    let y: i32 = 20;
    let use_y = move |n: i32| y + n;
    println(use_y(5));

    return 0;
}
